

load_setting <- function(this_setting = "Indonesia") {
  
  
  
  #CHECK inputs
  # #if (! this_setting %in% unique()){  #English names
  #   if (! this_setting %in% unique()){ #IDN names
  #     stop()
  #   }
  # }
  
}